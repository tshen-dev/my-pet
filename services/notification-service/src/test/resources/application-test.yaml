keycloak:
  enabled: false

spring:
  servlet:
    context-path: /notification-service
  cloud:
    config:
      enabled: false
  kafka:
    bootstrap-servers: ${KAFKA_URL:http://localhost:9092}
logging:
  level:
    root: ${LOG_LEVEL:info}